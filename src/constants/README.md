# Constants Folder (`src/constants/`)

## Purpose
Exposes read-only configuration variables, routes mappings, and enum variables.

## Contents
- `api.js`: Defines `API_ENDPOINTS` paths for all resources matching the backend router rules.
